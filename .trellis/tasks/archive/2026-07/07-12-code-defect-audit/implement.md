# 缺陷排查执行计划

1. 收紧后端配置、上传白名单和订阅 schema 的输入契约，并更新调用点。
2. 拆分 toast 发布函数并实现一次性 Axios token refresh/retry 流程。
3. 为修复的后端契约及刷新流程添加可自动运行的回归测试。
4. 运行前端 lint/build、后端测试及编译检查，复核跨层类型与错误处理。

## 验证命令

- `npm run lint`、`npm run build`（`frontend/`）
- 后端 Python 编译/导入检查，以及新增回归测试

## 风险与回滚

每项产品改动可独立回滚；不得触及用户已有的未提交文件。
