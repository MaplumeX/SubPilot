# Audit Report (Re-run) — SubPilot Frontend

**Target**: `frontend/src`
**Date**: 2026-07-18 (post-fix re-run)
**Prior score**: 15/20

---

## Audit Health Score

| # | Dimension | Score | Key Finding |
|---|-----------|-------|-------------|
| 1 | Accessibility | 3 | 排序表头已键盘可操作；Switch hit area 扩大；auto_renew 有 aria-label。残余：icon-xs 24px 刚达 AA 下限 |
| 2 | Performance | 3 | 无变化；整体优化良好 |
| 3 | Responsive Design | 3 | 移动 nav hamburger 收起；Calendar 事件标签 12px base。残余：表格在窄屏仍横向滚动（可接受） |
| 4 | Theming | 4 | 无变化；全 token 系统 |
| 5 | Anti-Patterns | 4 | 无变化；无 AI slop tells |
| **Total** | | **17/20** | **Good → 接近 Excellent** |

**Score change: 15 → 17 (+2)**

---

## 修复验证

| 上次问题 | 状态 | 验证 |
|----------|------|------|
| [P1] Switch 触控目标 18.4×32px | ✅ 修复 | `after:-inset-x-4 -inset-y-4` → hit area 50.4px，满足 AAA 44px |
| [P1] Calendar 10px 事件拥挤 | ✅ 修复 | `text-xs sm:text-[10px]` → 移动端 12px，桌面 10px |
| [P1] Nav 移动端无收起 | ✅ 修复 | 桌面 `hidden sm:flex` + 移动 hamburger `sm:hidden` |
| [P1] 排序表头不可键盘操作 | ✅ 修复 | `<TableHead>` 内 `<button type="button">` + `focus-visible:ring-3` |
| [P2] auto_renew 仅图标 | ✅ 修复 | `aria-label` + `role="img"` in table + card |
| [P2] logo 搜索按钮无 aria-label | ✅ 修复 | `aria-label={t("subscriptionForm.selectLogo")}` |
| [P2] Statistics loading 不一致 | ✅ 修复 | 一行文字 → skeleton（h-72 + 2× h-64 pulse） |
| [P2] Statistics empty 无 CTA | ✅ 修复 | 无引导 → icon + 说明 + "添加订阅" Button link |
| [P2] SettingsPage 卡片宽度不一致 | ✅ 修复 | 5 张卡统一 `max-w-lg` |
| [P2] Nav 移动端收起 | ✅ 修复 | hamburger dropdown |
| [P3] heading clamp → fixed rem | ✅ 修复 | `text-2xl`，0 个 clamp 残留 |
| [P3] out-of-month /35 对比度 | ✅ 修复 | `/35` → `/50` |
| [P3] ConfirmDialog 无错误反馈 | ✅ 修复 | `await onConfirm` + inline error + submitting 态禁用关闭 |

---

## 残余问题（未修复）

### [P2] icon-xs 按钮 24px 触控目标
- **Location**: `components/ui/button.tsx:30` — `size-6` (24px)
- **Status**: 刚达 WCAG 2.5.8 AA 下限（24px），不达 AAA 44px
- **Impact**: 日历 prev/next、表格行删除等 icon-xs 按钮在移动端偏小
- **Note**: Switch 通过 `after` 伪元素扩大 hit area 的策略可复制到 icon 按钮，但当前 hit area 仅 24px（无 after 扩展）

### [P3] 检测器 advisory（6 个，全 false positive / 刻意决策）
- `button.tsx` `0.8rem`、`CalendarPage.tsx` `10px` — 刻意紧凑尺寸，建议在 DESIGN.md 登记 `caption`/`micro` 层级
- `index.css` `rgba(0,0,0,…)` — ambient shadow token（DESIGN.md §4 已记录），检测器不解析 CSS 注释

---

## Anti-Patterns Verdict

**Pass.** 无变化。无 AI slop tells。

---

## Positive Findings（新增）

1. **键盘可访问性显著提升** — 排序表头 button 化 + auto_renew aria-label + logo 按钮 aria-label，WCAG 2.1.1 和 1.1.1 现已满足
2. **移动端体验完整** — hamburger nav + 12px 事件标签 + Switch hit area，移动端三大问题全解决
3. **状态一致性统一** — 4 个页面 loading/empty 词汇统一（skeleton + icon + CTA）
4. **错误恢复改善** — ConfirmDialog 不再 fire-and-forget，async 失败有 inline error + dialog 保持 open

---

## Recommended Next Steps

残余问题均为 P2-P3，无 P0/P1：
1. **[P2] `/impeccable adapt`**: icon-xs 按钮加 `after` 伪元素扩大 hit area（复用 Switch 策略）
2. **[P3]**: 在 DESIGN.md 登记 `caption`/`micro` 字号层级 + shadow token，消除检测器 advisory

> 修复后重新运行 `/impeccable audit` 看 18-19/20。